{
  "scripts": {
    "format": "prettier --cache --write .",
    "test": "pnpm install --resolution-only",
    "add-test": "node add.js"
  },
  "type": "module",
  "prettier": "@sanity/prettier-config",
  "devDependencies": {
    "@sanity/prettier-config": "^1.0.2",
    "prettier": "^3.2.5"
  },
  "packageManager": "pnpm@9.0.4",
  "dependencies": {
    "execa": "^9.1.0"
  }
}
